# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog and this project follows Semantic Versioning.

## [Unreleased]

### Added
- Added `tox.ini` for local and CI test automation.
- Added this `CHANGELOG.md` to track project changes.
- Added `MANIFEST.in` to include core packaging files in source distributions.

### Changed
- Improved packaging metadata in `setup.py` (classifiers, `python_requires`, license metadata).
- Simplified `pyproject.toml` build requirements for standards-compliant builds.
- Updated installation instructions in `README.md` for PyPI and Python version alignment.

## [1.3.3]

### Notes
- Existing project release baseline before changelog tracking started.

